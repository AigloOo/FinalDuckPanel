[build]
builder = "nixpacks"
buildCommand = "npm install && npm run build"

[deploy]
startCommand = "npx prisma migrate deploy && npm run db:seed && node server.js"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
